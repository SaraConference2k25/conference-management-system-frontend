name: Trigger Netlify SSR Build

on:
  push:
    branches:
      - master

jobs:
  trigger-build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Netlify Build
        run: curl -X POST -d {} ${{ secrets.NETLIFY_BUILD_HOOK }}
